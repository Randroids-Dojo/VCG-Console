# Owner questions: OAK-D Pro W comparison — 2026-07-25

The I-042 plan remains blocked without these inputs. Safe defaults grant no
purchase, camera operation, firmware/pipeline mutation, IR emitter use,
participant collection or temporary-image authority.

## OAK-001: exact candidate and purchase boundary

Should only USB OAK-D Pro W IMX378 SKU `A00573` enter the first screen, and who
may obtain a fresh destination-aware delivered quote or authorize purchase?

Safe default: retain `A00573` as a no-purchase candidate. The 2026-07-25 US
$529 list observation is stale at purchase time and excludes tax, shipping,
mount, power and integration.

## OAK-002: received-device identity

Which product, PCB/sensor revisions, calibration, firmware/bootloader, serial-
to-private-digest handling, included cable/Y-adapter and visible condition facts
must be recorded from a received unit?

Safe default: require exact received identity without publishing the serial.
Reject damaged, opened, modified, substituted or unofficially flashed devices.

## OAK-003: target, USB and power topology

Which exact ordinary x86 Linux, SteamOS and Pi ports/controllers, cables, hubs,
5 V supply/Y-adapter and concurrent USB loads define each blocking topology?

Safe default: externally power the Pro candidate through the vendor-supported
topology and measure wall/device/USB voltage, current, spikes and brownouts.
Enumeration cannot qualify the pipeline.

## OAK-004: mount and RGB/depth overlap geometry

How are shared UVC and OAK optical centers, axes, field-of-view, parallax,
calibration and five room placements co-registered without one camera blocking
or changing the other's view?

Safe default: freeze a measured rigid mount and independent geometry protocol.
Report RGB, valid-depth and action-qualified overlap separately.

## OAK-005: DepthAI runtime and lifecycle

Which exact DepthAI release/source commit, dependencies, firmware/bootloader,
udev/service permissions, offline package, pipeline graph, restart/update and
rollback path applies on every target?

Safe default: no `latest` dependency or network-fetched runtime. Pin every byte,
run unprivileged, retain license/SBOM evidence and fail closed on mismatch.

## OAK-006: depth-fusion algorithm

Which versioned algorithm consumes RGB/stereo depth for pose, floor contact,
occlusion or recovery, and how does it preserve Motion `0.4.0` semantics?

Safe default: keep the algorithm null. OAK depth cannot label itself, and OAK
on-device neural inference remains outside blocking camera/depth attribution.

## OAK-007: cross-camera and RGB/depth synchronization

Which exposure-authoritative instrument measures shared-UVC/OAK skew and OAK
RGB/depth alignment, what maximum skew applies, and which drift or missing-
timestamp conditions invalidate a cell?

Safe default: same session is not same exposure. Require internal OAK volatile
same-exposure fan-out where possible, record actual cross-camera skew and keep
the maximum gate null until uncertainty is approved.

## OAK-008: passive, active-dot and flood-assisted lanes

Which exact stereo modes, resolutions, FPS, filters, confidence, left-right
checks, emitter intensities and central/mono streams define passive, active-dot
and low-light flood lanes?

Safe default: keep all three separate. Flood/mono IR is exploratory and cannot
rescue RGB, passive or active-dot failure.

## OAK-009: IR safety and interference

Who reviews received-unit Class 1 documentation, participant/child use,
intensity, distance, duration, reflections, temperature, damage checks,
independent emitter-state observation and I-045 sunlight/other-IR scenarios?

Safe default: emitters stay off. Vendor classification cannot replace project
room/participant/legal review, and successful depth cannot waive safety.

## OAK-010: one-player and overlap staging

Does full I-042 selection require all 90,000 one-player and 13,500 conditional
two-player trials, and which completed I-054 gates authorize overlap sessions?

Safe default: yes for a complete depth-benefit claim. Finish one-player first;
do not recruit or run overlap pairs before I-054 authority.

## OAK-011: material-benefit and non-regression gates

What minimum valid-depth coverage/material improvement and maximum depth,
floor/contact, identity-switch, synchronization, overhead, power, temperature
and incremental-cost values justify depth?

Safe default: keep them null and freeze before collection. Require at least one
predeclared material benefit with no blocking regression; RGB camera-only
improvement is not depth benefit.

## OAK-012: independent ground truth

Which synchronized pose, floor/contact, overlap identity/action, lighting,
power, thermal and timing instruments provide truth without trusting candidate
depth, confidence, vendor claims or application summaries?

Safe default: bind calibrated independent instruments and uncertainty before
collection. Preserve invalid, missed, spurious, dropped and recovery events.

## OAK-013: data handling

May volatile same-exposure RGB/depth/IR fan-out or temporary diagnostic images
be used, who may access them, and how are memory clearing, retention and
deletion verified?

Safe default: no persistence or network egress. Release skeleton/depth summaries,
ground truth, timing, power and aggregates without names, portraits, voices,
addresses, stable participant IDs, device serials or free text.

## OAK-014: participants, operators and schedule

Who authorizes room access, adult consent, child consent/assent/comprehension,
camera and emitter operation, safety stop, randomized lane order, rests,
invalid reasons, termination, scoring and the exact schedule?

Safe default: no physical session. Separate emitter/safety operation from
ground-truth scoring, honor immediate stop requests and retain every failed,
invalid or incomplete cell.
