# Owner questions: x86 baseline follow-on — 2026-07-24

I-013 can close at the development-host boundary without these answers. The
questions below control destructive or hands-on follow-on work and reserve
Q-203 and Q-204 after the storage tranche's Q-199 through Q-202.

## Q-203: native-Linux disk and recovery authority

May a separate-disk native x86-64 Linux plan be prepared for this workstation,
and which physical disk may eventually be erased only after its identity,
backup state, boot role, and recovery media are reviewed with you?

Why this is needed:

- I-207 requires ordinary native Linux; Windows and WSL2 cannot supply that
  evidence.
- The prior inventory found two physical 2 TB disks, but neither disk is
  authorized for modification.
- Selecting the wrong disk could destroy the current Windows system or
  unbacked-up data.

Proposed default:

1. prepare a read-only disk/firmware/driver/recovery checklist;
2. identify the intended non-boot disk with you in person;
3. verify backups and Windows recovery media;
4. propose a pinned Linux image and partition plan; and
5. request a separate explicit authorization before any repartition, format,
   boot-order, Secure Boot, or OS installation change.

No disk has been modified.

## Q-204: hands-on camera and controller session

When should the physical I-211 session run, and which standards-conformant
controller should be connected or borrowed?

Why this is needed:

- the C920 enumerates as present, but real capture, exposure/timestamp,
  sustained worker, room-edge, and skeleton-only evidence require a person in
  the room;
- no controller is currently detected, so USB/Bluetooth/receiver mapping,
  disconnect/reconnect, Back/Pause, and recovery cannot be qualified; and
- reboot, suspend/resume, hosted-game, and long-soak observations also require
  hands-on access.

Proposed default:

- use the present C920 for a no-purchase compatibility pass;
- retain no raw video by default and export only bounded skeleton/timing
  evidence;
- use any already-owned SDL-standard controller over USB first;
- do not buy a camera or controller merely to answer this question; and
- keep the session explicitly separate from native-Linux, final-camera, room,
  and 120 ms end-to-end qualification.
