# Owner questions: Motion bridge target stall campaign

The Windows child-process observation can expose one desk behavior, but it
cannot select the product resource budgets or failure campaign.

## Q-241: target stall, suspend, and recovery gates

For the exact Raspberry Pi and ordinary x86-64 Linux assemblies, what
wall-clock soak duration and repetitions, producer/player/profile rates,
concurrent game/client count, native IPC choice, tracker/renderer/compositor
process topology, OS suspend/kill/crash injection methods, RSS/PSS/CPU/thermal
and scheduler/queue/drop limits, expiry/reconnect deadlines, Home/Back/Pause
availability, full latency gates, and independent review are required before
I-084 may close?

Safe default: retain one-frame ACK backpressure, per-session expiry, bounded
telemetry, and fail-closed health, but do not call the Windows Node result a
target, browser-suspend, tracker-isolation, native-IPC, performance, memory,
latency, or reliability qualification. A stalled or dead game must never
block the shell, reserved recovery controls, another healthy client, tracker
restart, or deterministic cleanup.
