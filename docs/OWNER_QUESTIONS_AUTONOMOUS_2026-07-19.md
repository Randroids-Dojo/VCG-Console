# Owner questions from the autonomous tranche

Last updated: 2026-07-19

No owner decision blocks the software-only Motion SDK work completed in this tranche.

The following inputs are intentionally deferred until they are convenient; neither should interrupt autonomous repository work:

1. When a camera session is practical, which time window should be used for the consented real-room one-player run in I-210? The prepared test must record only skeletal evidence by default and must not claim exposure-to-action latency from browser capture-arrival timestamps.
2. When the secondary x86-64 workstation is available, should its Windows qualification be run locally by you from `docs/WINDOWS_QUALIFICATION.md`, or will the repository be made available there for an agent-driven run? The bootstrap and inventory scripts already gather the machine facts, so no hardware specification needs to be typed manually.

Everything else encountered had an existing decision, a reversible engineering default, or an evidence-based way to proceed without owner input.
